# Generated by conan2toml. Do not hand-edit; regenerate from upstream
# conanfile.py instead.
#
# TODO(conan2toml): manual review needed for:
#   - build(): custom build steps - map to a cppup build plugin
#   - package(): custom install/copy steps - typically covered by CMake install
#   - package_info(): cpp_info populated imperatively - verify [[exports]]
#   - validate(): refuses unsupported settings - encode as [platforms] constraint
schema = 1

[package]
name = "spdlog"
version = "1.17.0"
kind = "library"
description = "Fast C++ logging library"
license = "MIT"
homepage = "https://github.com/gabime/spdlog"
repository = "https://github.com/conan-io/conan-center-index"
cppup_compat = ">=0.5.0"

[source]
type = "tar"
url = "https://github.com/gabime/spdlog/archive/v1.17.0.tar.gz"
checksum = "sha256:d8862955c6d74e5846b3f580b1605d2428b11d97a410d86e2fb13e857cd3a744"

[build]
system = "cmake"
args = []

[features]
fPIC = { default = true, description = "Position-independent code" }
header_only = { default = false, description = "Header-only mode" }
no_exceptions = { default = false, description = "Conan option: no_exceptions" }
shared = { default = false, description = "Build as shared library" }
use_std_fmt = { default = false, description = "Conan option: use_std_fmt" }
wchar_console = { default = false, description = "Conan option: wchar_console" }
wchar_filenames = { default = false, description = "Conan option: wchar_filenames" }
wchar_support = { default = false, description = "Conan option: wchar_support" }

[dependencies]
fmt = "=12.1.0"

[platforms]
os = ["linux", "macos", "windows"]
arch = ["x86_64", "arm64"]
